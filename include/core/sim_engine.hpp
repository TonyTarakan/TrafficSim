#pragma once

// Owns the simulation world and drives it forward one fixed step at a time.

namespace ts {

// TODO: struct SimConfig — fixed_dt, thread count
// TODO: class SimEngine — tick(), world(), snapshot publishing

}  // namespace ts