#pragma once

// Traffic signals, road signs, and speed limit resolution.

namespace ts {

// TODO: enum class SignalPhase — Green / Yellow / Red
// TODO: struct TrafficSignal
// TODO: enum class SignType — SpeedLimit / Yield / Stop / NoEntry
// TODO: struct RoadSign
// TODO: float effective_speed_limit(...) — resolves signs + signals for a lane position

}  // namespace ts