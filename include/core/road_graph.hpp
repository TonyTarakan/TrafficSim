#pragma once

// Road network primitives: nodes, lanes, and the graph connecting them.
// Supports multi-level interchanges via elevation (z) on nodes.

namespace ts {

// TODO: struct RoadNode — junction point (position, elevation level)
// TODO: struct Lane — directed segment between two RoadNodes
// TODO: class RoadGraph — adjacency structure + pathfinding (A*)

}  // namespace ts