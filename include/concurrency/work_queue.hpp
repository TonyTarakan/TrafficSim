#pragma once

// Chase-Lev work-stealing deque: owner pushes/pops from bottom (LIFO),
// thieves steal from top (FIFO).

namespace ts {

// TODO: class WorkQueue — push(), pop(), steal()

}  // namespace ts