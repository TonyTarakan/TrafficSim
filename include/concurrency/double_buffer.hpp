#pragma once

// Lock-free double buffer for passing WorldSnapshot from sim thread to render thread.

namespace ts {

// TODO: template<typename T> class DoubleBuffer — back(), swap(), front()

}  // namespace ts